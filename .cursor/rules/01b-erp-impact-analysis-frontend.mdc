---
description: ERP 跨模組影響分析（前端/文件變更時套用；高風險才完整）
alwaysApply: false
globs: js/**
---

# ERP Impact Analysis（Frontend / docs）

當你正在修改前端 `js/`（或後續新增的前端規則檔）且任務被判定為 A/B 級時，仍需做 impact analysis。  
最小必答（精簡）：
- 這個變更會影響哪個「使用者操作」與哪個「後端 action/routing」？
- 是否改到金額/數量/狀態/權限/POST-VOID（若是 → Task Level A）？
- 會影響哪些模組：Shipment / AR / Dealer / Promotion / Inventory / 月結？
- 需要補哪些測項（Happy / Retry / relevant concurrency）？

完整檢核清單請參考 `01-erp-impact-analysis.mdc`（server 側）。

