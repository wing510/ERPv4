---
description: ERP 全模組工程治理（分級 Gate：高風險才強制完整）
alwaysApply: true
---

# ERP Master Engineering Governance（治理路由器）

本檔是「治理路由器」：先判定 Task Level（A/B/C），再決定要跑哪些 Gate。

原則：高風險規則覆蓋「能直接實作就先做」。

## 分級 Gate（核心規則）

### Task Governance Level（A/B/C）

此處的 A/B/C 是 **Task Level**（是否需要完整 Gate），不是風險嚴重度。  
風險嚴重度請用 `BLOCKER/HIGH/MEDIUM/LOW`（見各 Gate 輸出）。

#### A = Critical（強制完整 Gate）
A 級僅限：
- **金額 mutation**
- **庫存/數量/餘額 mutation**
- **POST/VOID/CANCEL** 且具有財務效果
- **Payment / Refund / Write-off**
- **Settlement / Cumulative / Credit**
- **Permission-sensitive mutation**
- 或 **跨 2 個以上核心寫庫模組**（例如 Inventory + AR、Shipment + Cumulative）

#### B = Controlled（簡化 Gate）
- 單模組一般 mutation
- 非核心狀態變更
- API routing
- 一般資料維護

#### C = Fast Path（快速通道）
- 純 UI（不改資料/權限/狀態/計算語意）
- 文字
- 樣式
- 唯讀查詢
- 不改計算語意的報表呈現

**低風險快速通道（明確例外）**：
- 文字修正（錯字、用語、提示文案）
- CSS/排版調整、間距、顏色（不影響資料與權限）
- 只改「顯示」不改「計算/寫入/狀態」的 UI（例如欄位顯示順序、顯示格式）

低風險輸出模板（固定四行；Task Level = C）：
- Task Level: C
- Risk Severity: LOW
- Impact: <一句話說明影響範圍>
- Smoke: <一步操作驗證 + 成功判斷>
- Gate Result: GO FOR IMPLEMENTATION / NO-GO

**注意**：若改到金額/數量/狀態/權限/SQL/RPC/作廢鏈，即使看起來很小也不是低風險。

## B 級最小交付物（Task Level = B）
- Direct / Data / Compatibility Impact（01）
- Lock/Retry behavior（若適用；03）
- Routing / Error mapping（若適用；07）
- Happy Path
- Retry test
- 至少 1 個 relevant concurrency test（若有 shared mutation）

## Unknown 分級（避免把開發卡死）
- UNKNOWN_BLOCKER：涉及金額、庫存、出入帳、權限、POST/VOID → 停止，先決策
- UNKNOWN_NONBLOCKER：命名、顯示、非核心欄位 → 可繼續，但需記錄

## Gate Result Dictionary（唯一字典）
- **NO-GO**：Blocker 未解，不得進入實作/測試
- **GO FOR IMPLEMENTATION**：可進入實作（可建立 branch、寫 code）
- **GO FOR TESTING**：可進 DEV/測試環境驗證（含 migration preflight、parity、並行）
- **GO FOR PRODUCTION**：可上正式（需完成指定測試與回滾/備份）

## Fallback 衝突處理（統一規則）
- 預設禁止 silent fallback
- 僅允許「人工核准過渡方案」，且必須記錄：
  - Compatibility（07）
  - Parity（06）
  - Test（09）
  - Removal Plan（何時移除過渡）

## Repo 提醒：`.cursor/rules` 可能被 gitignore

本 repo 預設忽略 `.cursor/`。若要讓團隊與 CI 共享治理規則，需：

- git 認可方式 A：`git add -f .cursor/rules/*.mdc`
- 或方式 B：調整 `.gitignore` 允許納入 `.cursor/rules/`

