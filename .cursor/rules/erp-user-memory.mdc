---
description: 使用者口述「記住」的決策與偏好（AI 自動維護）
alwaysApply: true
---

# ERP 使用者記憶（自動儲存）

> 觸發：使用者說「記住」或「你記住」→ AI **必須**把該則決策/規則/偏好寫入本檔（不可只口頭答應）。
> 儲存位置：`.cursor/rules/erp-user-memory.mdc`（取代已刪除的 `docs/ERP_對話續接記憶.md`）。

## 記憶條目

### 「新的一天」口令
- 使用者說 **「新的一天」** 時：用新手模式逐步引導本機 DEV 開工。
- 流程：
  1. **開 PowerShell** → 貼：`cd d:\Desktop\ERP\server; npm run dev`
     - 成功：`ERP Supabase API http://127.0.0.1:8787/exec`、`env=DEV`；視窗保持開著
     - 若 `EADDRINUSE`：`netstat -ano | findstr ":8787"` → `Stop-Process -Id <PID> -Force` → 重跑
  2. **開瀏覽器** → `http://127.0.0.1:5501/index.html` → **Ctrl+F5**
  3. **admin 重新登入**（API 重啟後 session 失效）
     - 成功：進 Dashboard，Console 無 `Failed to fetch`

### 記住 → 自動儲存
- 使用者說 **「記住」** 或 **「你記住」** 時：AI 須**自行寫入本檔**新增一則條目（含日期），不可只口頭答應。
- 未說「記住」時：不要擅自新增本檔內容。

### SQL 檔名／查詢名（一律中文）
- 格式：**`v4.1.XX_中文描述.sql`**（XX = 步驟編號，如 00、01、10、18）。
- Supabase SQL Editor 儲存查詢名：與檔名一致或同義中文（如 `v4.1.18_取消資料API公開`）。
- 範例：`v4.1.00_正式庫一次建置.sql`、`v4.1.10_商業發票.sql`、`v4.1.18_取消資料API公開.sql`。

### Supabase Table Editor：不要地球圖示（不公開 Data API）
- `public` 表不應對 `anon` / `authenticated` 開放（Table Editor 列表旁不出現地球圖示）。
- ERP 僅 **Node API + service_role** 存取 DB；瀏覽器不直連 Data API。
- 新建／正式庫建表後，應提供或提醒執行 `server/sql/v4.1.18_取消資料API公開.sql`。
- **不可** REVOKE `service_role`（否則 Node 後端無法讀寫）。

### 對話用字（一律繁體）
- 與使用者對話時：**一律使用繁體中文**，**不可出現簡體字**（含說明、表格、定案摘要、教學步驟）。

### 說明方式：白話優先，不要只丟程式欄位名
- 與使用者**討論、定案、教學**時：以**畫面上看得懂的中文**說明（例如「退回原因」「收回日期」「備註」「單價」）。
- **不可**只列 `return_reason`／`return_date`／`remark`／`unit_price` 這類英文欄位名當主要內容；使用者看不懂。
- 若需對照資料庫／程式：英文欄位名放**次要位置**（括號或附錄），且先有一句白話解釋該欄「要填什麼、拿來幹嘛」。
- 表格、規格摘要優先寫：**欄位中文名｜使用者要填什麼｜系統拿來做什麼**。

### 寫說明／規格：以程式為準，md 僅參考（2026-07-05）
- 使用者請 AI **寫說明、說明書、規格摘要**（含對話內文、存 `docs/`）時：
  - **唯一真相**：現行程式（`js/`、`server/src/`、`server/sql/`）、`js/help.js`、畫面行為。
  - **`docs/*討論定案*.md`**：僅作**意圖、名詞、流程骨架**參考；實作常已修正，**不可盲從**。
  - 若 md 與程式矛盾：以程式為準；說明中可標「定案原意 vs 現行實作」，或先問使用者再定稿。
  - 寫完建議標**日期**與**實作狀態**（已做／未做），避免沿用舊 md 的「待開發」。

### 後端改動後：重啟提醒格式
- 後端（`server/`）有改動、需重跑 Node API 時：**不要**寫長篇逐步教學。
- **可複製的程式碼區塊內不可出現「重啟後端」**（使用者一鍵複製會多貼無用字）；說明文字可寫在區塊外。
- **固定格式**：區塊外一句說明（如「請重啟後端並 Ctrl+F5」）＋區塊內**僅一行指令**：

```
cd d:\Desktop\ERP\server; npm run dev
```

- 「新的一天」口令仍保留完整步驟；一般修 bug／新功能後端改動用此簡短格式即可。

### 開工／動模組前：先對齊 codebase 與定案書（2026-07-01）
- **適用**（動手改程式**之前**必做）：
  - **增加新功能**
  - **跨模組修改**（牽涉 2 個以上模組，例如前端頁 + 後端 bundle、結算 + AR、Dealer + 客戶主檔、任一功能 + `server/sql/`）
  - **改商業規則／口徑**（請款淨額、折讓、作廢順序、回饋與累積等）
  - **動到 SQL 遷移或 RPC**
- **可不完整掃描**（可直接改）：
  - 單檔、單點小修（錯字、樣式、按鈕文案），且不碰商業邏輯
  - 使用者明確說「先討論／先不要改程式／只要建議」
  - 純問答、不寫程式
- **要做的事**（不必全庫掃完，但須針對**相關**範圍）：
  1. 讀或搜尋 `docs/*定案*.md`（及 `.cursor/rules/erp-user-memory.mdc`）中與任務相關段落
  2. 搜尋相關 `js/`、`server/src/bundles/`、`server/sql/`（grep／讀檔）
  3. 對齊後再實作；**優先於**「能直接實作就先做」（僅在上述適用情境）
- **開工／續接**（含「新的一天」之後的第一個開發任務）：同樣先對齊 codebase + 定案書，再動手。

### 新增表/畫面：同步補對應關係與按鈕（2026-07-07）
- 以後**新增功能**若有：
  - **新增表**（`server/sql/` 建表）或
  - **新增畫面**（`modules/*.html`）
  則需同步：
  - 更新 `js/core/migration-stub.js` 的 `MODULE_TABLES`（表 → 對應模組）
  - 在對應畫面「標題列」自動加上 `openSheetLink('<table_key>')` 按鈕（比照既有 `btn-sheet` / `btn-sheet-alt` 樣式）

## 變更紀錄
- 2026-05-24：建立本檔；併入「新的一天」口令；`docs/ERP_對話續接記憶.md` 已刪除。
- 2026-05-24：使用者要求「記住」時 AI 自行儲存至本檔。
- 2026-05-24：SQL 檔名／查詢名一律中文；Supabase 表不公開 Data API（無地球圖示）。
- 2026-05-24：SQL 檔名統一為 `v4.1.XX_中文.sql`（`server/sql/` 已更名）。
- 2026-06-16：對話一律繁體中文，不可出現簡體字。
- 2026-06-16：討論／說明以白話與中文欄位為主，不可只給英文程式欄位名。
- 2026-06-16：後端改動後重啟提醒固定兩行：「重啟後端」+ `cd d:\Desktop\ERP\server; npm run dev`（不可只寫四個字）。
- 2026-06-30：可複製區塊內不要「重啟後端」四字，僅保留 `cd d:\Desktop\ERP\server; npm run dev`；說明寫在區塊外。
- 2026-07-01：開工／新功能／跨模組／商業規則／SQL 修改前，須先搜尋對齊 `js/`、`server/src/bundles/`、`server/sql/` 與 `docs/*定案*.md`；單檔小修可例外。
- 2026-07-05：寫說明／說明書時以程式為準，`docs/*定案*.md` 僅參考；矛盾時以現行實作為準。
- 2026-07-07：新增表/畫面時，同步更新 `MODULE_TABLES` 並在畫面標題列加 `openSheetLink` 按鈕。
